rules:
  - capability: shell
    effect: allow
    effect: allow
  - capability: web_fetch
    effect: allow
  - capability: web_search
    effect: allow
  - capability: fs_read
    effect: allow
  - capability: fs_write
    effect: allow
  - capability: shell
    effect: allow
